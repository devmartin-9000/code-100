import { Button } from "./ui/button";

const Games = () => {
    return ( 
      <section id="games" className="bg-background h-screen">
            <div className="items-center justify-center flex fle-col text-center h-screen">
                <Button>Games</Button>
          </div>
      </section>
  )
}

export default Games